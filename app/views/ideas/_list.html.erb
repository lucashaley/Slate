<li class="list-group-item d-flex justify-content-between align-items-start">
  <div class="ms-2 me-auto"><%= truncate(idea.title, length:60) %></div>
  <span class="badge bg-primary rounded-pill"><%= idea.comments_count %></span>
</li>
